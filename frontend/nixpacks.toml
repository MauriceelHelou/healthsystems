[variables]
DISABLE_ESLINT_PLUGIN = "true"

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build --listen tcp://0.0.0.0:${PORT:-3000}"
